table "dbs_2dev"."employees" {
  columns = (
    column "EmployeeID" {
      type = int(11)
      null = NOT NULL
    }
    column "FirstName" {
      type = varchar(50)
    }
    column "LastName" {
      type = varchar(50)
    }
    column "Position" {
      type = varchar(50)
    }
  )
  primaryKey = {
    columns = [
      EmployeeID
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

